"""File indexing and MCP tools.

Makes ``.3mf`` projects and G-code files answerable without ever putting them in the model's
context: two indexers and the ``project``/``gcode`` tool surfaces, each bounding its own response
([file_indexing.md](../../docs/design/file_indexing.md)).
"""
